{
  "dependencies": {
    "axios": "^0.17.1",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.17"
  },
  "engines": {
    "node": "8.5.0",
    "npm": "5.6.0",
    "yarn": "1.3.2"
  },
  "scripts": {
    "heroku-postbuild": "cd client && npm install && run build"
  }
}
